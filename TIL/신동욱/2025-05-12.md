### Hailo용 모델로 얼굴 인식 성공한 방법
- 일단 Hailo는 `uint8`형태로 연산을 수행하기 때문에, float type의 실수형 연산을 하지 못한다
- 그런데 임베딩 벡터는 실수여야 한다
- 그래서 hailo 모델은 연산을 모두 정수로 하고, 나중에 후처리가 필수적이다
- 후처리를 직접해줘야 한다
- 만약 모델이 231의 값을 출력했다면, $(231 - 127.5) / 128$ 와같이 후처리를 해줘야 함